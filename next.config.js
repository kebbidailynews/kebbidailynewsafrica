/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // You can remove `unoptimized` to allow Next.js to optimize images
    // unoptimized: true,
  },
};

module.exports = nextConfig;
